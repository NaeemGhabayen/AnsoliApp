<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ansolienapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnsolienApp">
        <activity android:name=".Fragment.ReportActivity"></activity>
        <activity android:name=".Ui.alertActivity" />
        <activity android:name=".Ui.supHomeActivity" />
        <activity android:name=".Ui.homeActivity" />
        <activity android:name=".Ui.loginActivity" />
        <activity android:name=".Ui.signUpActivity" />
        <activity android:name=".Ui.registerActivity" />
        <activity android:name=".Ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>